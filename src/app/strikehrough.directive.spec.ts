import { StrikehroughDirective } from './strikehrough.directive';

describe('StrikehroughDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikehroughDirective();
    expect(directive).toBeTruthy();
  });
});
